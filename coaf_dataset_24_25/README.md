# Placeholder: CoAF Dataset

Put CoAF composed data and precomputed latents here. Data files are intentionally not committed.

For the current no-state0 640x480 v6 track path, `state_norm_stats.pt` and `action_norm_stats.pt` are not required. `track_norm_stats.pt` is expected inside the composed dataset directory.
